五级流水： 1、取指  2、译码  3、执行  4、访存  5、回写

1、取值阶段：PC模块(pc_reg.v),IF/ID模块(if_id.v)

2、译码阶段：Regfile模块,ID模块,ID/EX模块

疑问总结：
（1）regfile.v中输出的寄存器值的变量类型是reg，为什么？
答：输出的类型绝大多数为reg,是为了能存储上一个时刻的状态。
(2)always的敏感变量写成*，是任一输入变化都会触发这个always吗？
答：是的，每一点输入上的变化，都会引发整个模块的动作。
（3）读操作是组合逻辑电路，写操作是时序逻辑电路，why？（《自己动手写CPU》P68）
（4）id.v中关于指令是否有效的问题？